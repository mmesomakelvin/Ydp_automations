# JavaScript dependencies and generated build output
**/node_modules/
**/.vite/
**/.next/
**/dist/
**/coverage/
**/.vercel/

# Local environment files and logs
**/.env
**/.env.*
!**/.env.example
**/*.log

# Operating system files
**/.DS_Store
**/Thumbs.db

# Local isolated development worktrees
.worktrees/

# Browser automation output
**/.playwright-mcp/

# Holds the real site password — must never be committed
**/supabase/set-password.sql

# Real cohort data (participant names, emails, pairings) — must never be
# committed to a public repo. Run it in Supabase; keep it local only.
**/supabase/seed.sql
